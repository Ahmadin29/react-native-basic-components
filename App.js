import * as React from 'react';
import { View } from 'react-native';
import {Button, Text, Divider} from './src'

export default function App(params) {
  return(
    <View style={{
      padding:15,
    }} >
      <Divider
        color='red'
      />
    </View>
  )
}